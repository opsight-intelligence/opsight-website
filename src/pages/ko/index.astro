---
import Base from '../../layouts/Base.astro';
import Home from '../../components/Home.astro';
import { home } from '../../i18n/home';
const copy = home.ko;
---
<Base locale="ko" title={copy.title} description={copy.description} path="/" locales={['en', 'ko']} current="home">
  <Home copy={copy} locale="ko" />
</Base>
