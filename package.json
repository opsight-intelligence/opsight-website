{
  "name": "opsight-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/sitemap.mjs",
    "build": "astro build",
    "postbuild": "node scripts/check-dist.mjs",
    "preview": "astro preview",
    "check": "npm run build"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
